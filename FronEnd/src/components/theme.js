import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#46237A",
    },
    secondary: {
      main: "#e43360",
    },
    background: {
      default: "#FAFAFA",
    },
  },
});
